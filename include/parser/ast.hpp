#pragma once

#include <belt/class_macros.hpp>

namespace kuso {
class AST {
  DEFAULT_CONSTRUCTIBLE(AST)
  DEFAULT_COPYABLE(AST)
  DEFAULT_DESTRUCTIBLE(AST)
  DEFAULT_MOVABLE(AST)
 public:
 private:
};
}  // namespace kuso